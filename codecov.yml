# Codecov posts a coverage report comment on every PR, with no pass/fail
# verdict: whether new code needs more coverage is the PR author's
# judgement call, so no status comparison is configured.
coverage:
  status:
    project: off
    patch: off

comment:
  layout: "reach, diff, flags, files"
  require_changes: false
